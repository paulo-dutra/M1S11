import { CaptalizePhilipsPipe } from './captalize-philips.pipe';

describe('CaptalizePhilipsPipe', () => {
  it('create an instance', () => {
    const pipe = new CaptalizePhilipsPipe();
    expect(pipe).toBeTruthy();
  });
});
